import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "app.screeb.sdk.ionic.example",
  appName: "Screeb Ionic Example",
  webDir: "dist",
};

export default config;
